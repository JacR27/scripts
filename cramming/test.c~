#include <stdio.h>
#include <stdint.h>
#include <stdlib.h>
#define MAXNAMELEN 100
main()
{
  while (1){
    int32_t block_size;
    fread(&l_ref, sizeof(int32_t), 1, stdin);
    printf("%d",block_size);
    if (block_size == EOF)
      break;
  
  }
